# -Confidence-Correctness-diagram
This evaluation framework decomposes the probabilistic confusion matrix into a confidence–correctness matrix with four interpretable components: reliability (R), overconfidence (O), underconfidence (U) and ambiguity (A). The Class-specific Confidence–Correctness diagram is a visual representation derived from this class-specific decomposition.
